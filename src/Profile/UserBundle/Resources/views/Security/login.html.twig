{% extends '::base_light.html.twig' %}


{% block content %}
<div class="wrapper full-page-wrapper page-login text-center">
    <div class="inner-page">
        <div class="logo">
            <a href="{{ path('home') }}">logo
            </a>
        </div>
        <div class="social_auth">
            {% for owner in hwi_oauth_resource_owners() %}
                <a href="{{ hwi_oauth_login_url(owner) }}" class="btn btn-social btn-{{ owner }}"><i class="fa fa-{% if owner == 'vkontakte' %}vk{% else %}{{ owner }}{% endif %}"></i> {{ owner | trans({}, 'HWIOAuthBundle') }}</span></a>
            {% endfor %}
        </div>

        <div class="separator">
            <span>ИЛИ</span>
        </div>

        <div class="login-box center-block">
            {% if error %}
                <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}
            <form class="form-horizontal" action="{{ path("fos_user_security_check") }}" role="form" method="post">
                <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
                <p class="title">Use your username</p>
                <div class="form-group">
                    <label for="username" class="control-label sr-only">Username</label>
                    <div class="col-sm-12">
                        <div class="input-group">
                            <input type="text" placeholder="username" name="_username" id="username" class="form-control">
                            <span class="input-group-addon"><i class="fa fa-user"></i></span>
                        </div>
                    </div>
                </div>
                <label for="password" class="control-label sr-only">Password</label>
                <div class="form-group">
                    <div class="col-sm-12">
                        <div class="input-group">
                            <input type="password" placeholder="password" name="_password" id="password" class="form-control">
                            <span class="input-group-addon"><i class="fa fa-lock"></i></span>
                        </div>
                    </div>
                </div>
                <label class="fancy-checkbox">
                    <input type="checkbox" name="_remember_me">
                    <span>Запомнить меня</span>
                </label>
                <button class="btn btn-custom-primary btn-lg btn-block btn-login"><i class="fa fa-arrow-circle-o-right"></i> Войти</button>
            </form>

            <div class="links">
                <p>
                    <a href="{{ path('fos_user_change_password') }}">Забыли пароль?</a>
                </p>
                <p>
                    <a href="{{ path('fos_user_registration_register') }}">Зарегистрироваться</a>
                </p>
            </div>
        </div>
    </div>
    <div class="push-sticky-footer"></div>
</div>
{% endblock content %}
